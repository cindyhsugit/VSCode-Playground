# create a API endpoint with fastAPI

# document API endpoint with gui
# run uvicorn myAPIEndpoint.fastAPIExample:app --reload


# add test cases for the endpoint

# pytest fixture = reusable setup.

# pytest mock = pretend object.

# pytest patch = temporary swap.